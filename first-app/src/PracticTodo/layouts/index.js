import React from 'react';
import TodoComponentsContainer from '../components/TodoComponentsContainer'

const Layout = () =>{
    return(
        <TodoComponentsContainer/>
    );
};

export default Layout;